{
    "name": "my-workspace",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "concurrently \"npm run dev --prefix front\" \"npm start --prefix back\""
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    }
}
